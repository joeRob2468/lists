name: lists
services:
  db:
    image: postgres:15-alpine
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-lists_database}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: always
    env_file: 
      - ../.env
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-password}
      POSTGRES_DB: ${DB_NAME:-lists_database}
    volumes:
      - pgdata:/var/lib/postgresql/data

  api:
    image: ghcr.io/${GITHUB_USERNAME}/${PROJECT_NAME}-api:latest
    restart: always
    depends_on:
      db:
        condition: service_healthy
    env_file: 
      - ../.env 
    environment:
      DATABASE_URL: postgres://${DB_USER:-postgres}:${DB_PASSWORD:-password}@db:5432/${DB_NAME:-lists_database}

  web:
    image: ghcr.io/${GITHUB_USERNAME}/${PROJECT_NAME}-web:latest
    restart: always

  umami-db:
    image: postgres:15-alpine
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U umami"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: always
    environment:
      POSTGRES_USER: umami
      POSTGRES_PASSWORD: ${UMAMI_DB_PASSWORD:-umami_password}
      POSTGRES_DB: umami
    volumes:
      - umami-db-data:/var/lib/postgresql/data
  
  umami:
    image: ghcr.io/${GITHUB_USERNAME}/${PROJECT_NAME}-umami:latest
    restart: always
    depends_on:
      umami-db:
        condition: service_healthy
    env_file: 
      - ../.env
    environment:
      DATABASE_URL: postgresql://umami:${UMAMI_DB_PASSWORD:-umami_password}@umami-db:5432/umami
      APP_SECRET: ${UMAMI_APP_SECRET}
      TRACKER_SCRIPT_NAME: ${UMAMI_TRACKER_SCRIPT_NAME}
      BASE_PATH: ${UMAMI_BASE_PATH:-/stats}

  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: always
    command: tunnel --no-autoupdate run
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN}
    env_file: 
      - ../.env

  watchtower:
    image: containrrr/watchtower
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /root/.docker/config.json:/config.json
    command: --interval 60 --cleanup --debug

volumes:
  pgdata:
  umami-db-data: